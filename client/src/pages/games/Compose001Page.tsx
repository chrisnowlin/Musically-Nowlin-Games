import React from "react";
import { Compose001Game } from "@/components/Compose001Game";

export const Compose001Page: React.FC = () => {
  return <Compose001Game />;
};

export default Compose001Page;
